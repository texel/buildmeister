# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/buildmeister.rb'

Hoe.new('buildmeister', Buildmeister::VERSION) do |p|
  # p.rubyforge_name = 'buildmeisterx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
